using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class second-script : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //nothing doing
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
